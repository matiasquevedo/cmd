<%= form_with(model: food, local: true) do |form| %>
  <% if food.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(food.errors.count, "error") %> prohibited this food from being saved:</h2>

      <ul>
      <% food.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <label>Comida</label>
    <%= form.text_field :name, id: :food_name, class: "form-control" %>
  </div>
  <br>
  <div class="field">
    <label>Receta</label>
    <%= form.text_area :description, id: :food_description, class: "form-control", id: "receta" %>
  </div>
  <br>
  <div class="field">
    <label>Precio por Porción</label>
    <%= form.text_field :price, id: :food_price, class: "form-control" %>
  </div>
<!-- 
  <div class="field">
    <%= form.label :menu_id %>
    <%= form.text_field :menu_id, id: :food_menu_id, class: "form-control" %>
  </div>
 -->
  <br>
  <div class="field">
    <%= form.collection_select(:menu_id , Menu.all, :id, :name, {:prompt => 'Menu'}, {:class=>'custom-select'}) %>
  </div>
  <br>
  <div class="actions">
    <%= form.submit "Crear Comida", class: "btn btn-primary" %>
  </div>

  <script>
    $('#receta').trumbowyg();
  </script>
<% end %>
