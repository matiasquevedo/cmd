<%= form_with(model: sale, local: true) do |form| %>
  <% if sale.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sale.errors.count, "error") %> prohibited this sale from being saved:</h2>

      <ul>
      <% sale.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <label> Estado</label>
    <%= form.text_field :status, id: :sale_status, class: "form-control" %>
  </div>

  <div class="field">
    <label> Fecha de Entrega</label><br>
   <!--  <%= form.datetime_field :shippedDate, id: :sale_shippedDate, class: "form-control" %> -->
   <input type="text" data-provide='datepicker' >
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
